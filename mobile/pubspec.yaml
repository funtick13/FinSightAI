name: mobile
description: Минимальное Flutter‑приложение для проекта FinSight AI.
publish_to: "none" # Это приватный проект.

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true